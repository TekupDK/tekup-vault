## TekupVault Environment (Example)
#
# IMPORTANT: We use central secrets in Tekup repo:
#   ../../tekup-secrets/.env.shared
#
# This file is only a local fallback for developers. Prefer loading from tekup-secrets.
# API and Worker already try to load tekup-secrets automatically.

# Database (fallback for local dev)
DATABASE_URL=

# Supabase (ikke påkrævet for Vault)
# Bemærk: TekupVault bruger PostgreSQL via DATABASE_URL. Supabase‑felter her er kun
# bevaret for kompatibilitet i ældre setups og bør normalt ikke udfyldes.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

# GitHub
GITHUB_TOKEN=
GITHUB_WEBHOOK_SECRET=

# OpenAI
OPENAI_API_KEY=

# Server
PORT=3000
NODE_ENV=development
LOG_LEVEL=info
